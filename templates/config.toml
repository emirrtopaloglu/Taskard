[defaults]
permission_mode = "bypassPermissions"
report_max_lines = 15

[roles]
planner = "opus"
implementer = "sonnet"
reviewer = "opus"

[risky_operations]
patterns = ["migration", "deploy", "rm -rf", "drop table", "git push --force"]
